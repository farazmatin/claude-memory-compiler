{
  "$comment": "The SessionStart/PreCompact/SessionEnd hooks were removed along with the Claude Code conversation-capture system they drove. This pipeline is invoked directly (see README.md), not through hooks."
}
